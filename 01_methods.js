let bhavik = "Bhavik";
console.log(typeof bhavik.toUpperCase());
console.log(bhavik.toUpperCase());

let numbers = [1, 2, 3]
numbers.push(4); // it will push to the array.
numbers.push(5); // it will push to the array of numbers.
console.log(numbers);
console.log(numbers.pop()); // 5 will be popped from the list.
console.log(numbers);

/*
Array methods:
push(number to be pushed);
pop(); // no argument
*/